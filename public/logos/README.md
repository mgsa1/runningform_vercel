# RunningForm — Logo Propositions

All logos are designed for dark backgrounds (`#030712`). Open in a browser to preview.

| File | Concept |
|---|---|
| `logo-sprint.svg` | Geometric stick-figure runner with motion lines → bold wordmark. Clean and sporty. |
| `logo-stride.svg` | Filled runner silhouette with a blue swoosh underline. "Running" white + "Form" blue. |
| `logo-monogram.svg` | RF lettermark where the R's leg becomes a runner's stride. Full name in small caps below. |
| `logo-retro.svg` | Circular badge with arc text + runner inside. Stacked wordmark to the right. |
